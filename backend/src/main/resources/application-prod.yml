# Prod profile: Swagger disabled, minimal logging, session tables managed by Flyway.

server:
  port: 8081

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

logging:
  level:
    root: WARN
    com.glm: INFO
